{
  "name": "bridge-mcp-figma/websocket",
  "private": true,
  "scripts": {
    "dev": "partykit dev --live",
    "deploy": "partykit deploy"
  },
  "devDependencies": {
    "partykit": "^0.0.110"
  }
}
